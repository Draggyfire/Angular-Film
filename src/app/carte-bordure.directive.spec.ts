import { CarteBordureDirective } from './carte-bordure.directive';

describe('CarteBordureDirective', () => {
  it('should create an instance', () => {
    const directive = new CarteBordureDirective();
    expect(directive).toBeTruthy();
  });
});
